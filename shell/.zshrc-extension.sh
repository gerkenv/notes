# # Add timestamp to the command line prompt
# PROMPT='%{$fg[yellow]%}[%D{%f/%m/%y} %D{%L:%M:%S}] '$PROMPT

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='mvim'
# fi

# # Extend The `$PATH`
# export PATH=/Users/user-name/Library/Python/3.7/bin:$PATH
